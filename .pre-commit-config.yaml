# Example .pre-commit-config.yaml — copy this into your own repo.
repos:
  - repo: https://github.com/JeevaByte/inframind-ai
    rev: v0.1.0
    hooks:
      - id: infralint
        args: [--fail-on, high]
