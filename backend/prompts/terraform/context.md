Focus strongly on Terraform semantics.

Inspect providers, resources, IAM policies, public exposure, encryption posture, tagging, backup posture, and obvious cost inefficiencies.

If the file defines AWS networking, storage, IAM, or compute resources, call out the highest-impact operational and security issues first.